# 企业入职助手：项目升级导学

## 项目升级结论

面试官觉得“简单”，通常不是因为技术点少，而是当前演示的主任务停留在“查资料并回答”。项目应从“企业入职问答助手”升级为“新员工首周入职交付协调平台”：系统要对设备、账号、材料、培训等事项的状态、责任人、截止时间、异常和验收负责，而不只对一段回复负责。

## 前置知识（面试高频）

| 知识点 | 为什么需要 | 项目位置 | 高频度 |
| --- | --- | --- | --- |
| Agent / Workflow / Tool / Skill 边界 | 避免把固定流程包装成多 Agent | `server/planner.mjs`、`server/executor.mjs` | 极高 |
| 任务状态机 | 将问答升级为交付 | `data/onboarding_tasks.json`，待新增 Case/Task | 极高 |
| 读写权限与审批 | 让 Agent 能办事但不越权 | `server/tool-registry.mjs`、`server/risk-review.mjs` | 极高 |
| A2A 协作协议 | 多 Agent 必须有委托、回执、责任边界 | 待新增 Agent 注册与消息模块 | 极高 |
| RAG 可信性 | 制度问答要有来源、版本和无答案兜底 | `server/knowledge-rag.mjs` | 高 |
| 评测与 Trace | 定位路由、检索、执行和生成中的失败 | `server/evaluation.mjs`、`server/logger.mjs` | 高 |

## 先学习什么

| 亮点 | 价值 | 建议先读 | 读完能回答什么 |
| --- | --- | --- | --- |
| 受约束规划 | Planner 有白名单、依赖补全和必需能力校验 | `server/planner.mjs` | 为什么模型不能自由选 Tool？ |
| 确定性事实锚定 | 员工状态、任务、联系人由 Tool 返回，模型不能编造 | `server/executor.mjs`、`server/tool-registry.mjs` | Agent 如何降低幻觉？ |
| 风险与人工兜底 | 敏感信息不能只靠 Prompt 约束 | `server/risk-review.mjs`、`server/privacy.mjs` | 高风险请求如何拦截？ |
| 可追溯执行 | 每步有输入、输出、版本和 Trace | `server/executor.mjs`、`server/logger.mjs` | 回答错了怎么排查？ |
| 任务化升级 | 从一次回答变成长期状态与交付 | `data/onboarding_tasks.json`、`data/handoffs.json` | 什么使它成为 Agent Manager 项目？ |

自学提醒：若某文件或原理看不懂，请继续追问 AI；本导学负责给学习路径与题目，不提供逐行讲解。

## 升级后最小架构

```text
员工目标
  -> 入职协调 Agent：创建 Case、拆任务、选择责任 Agent、汇总结果
     -> HR Agent：材料核验 / 制度依据
     -> IT Agent：设备、账号、门禁工单与状态
     -> 培训 Agent：课程排期、报名、提醒
     -> 风险 Agent：权限校验、审批、人工转接
  -> 任务账本：状态、依赖、SLA、证据、责任人、审计
```

每个 Agent 都必须定义：身份和 Owner、目标、可用 Tool、可访问的数据域、输入输出 Schema、成功定义、成本/SLA、失败码和人工兜底。只有不同专业边界、权限边界、交付物或并行价值明确时才拆 Agent；固定校验和审批仍保持 Workflow。

## 建议新增的数据对象

| 对象 | 核心字段 | 目的 |
| --- | --- | --- |
| Case | id、员工、总目标、优先级、状态、截止时间 | 记录一次完整入职请求 |
| Task | id、case_id、负责人、依赖、状态、SLA、验收物 | 作为系统真实状态 |
| Agent Profile | id、版本、Owner、能力、权限、SLA | 明确谁能做什么 |
| A2A Message | task_id、发送方、接收方、授权、输入证据、期望产物、幂等键 | 结构化委托与回执 |
| Approval | 动作、风险级别、审批人、决定、理由 | 写操作及敏感操作可控 |
| Artifact | 工单号、材料校验结果、课程报名记录等 | 验收而不是“自称完成” |

## 写操作与风险策略

- 低风险：查询制度、查待办，可自动执行。
- 中风险：创建模拟 IT 工单、提醒负责人，需要身份校验、频率限制和幂等键。
- 高风险：账号权限、合同、薪资、个人敏感信息，必须策略拦截并转人工审批。
- 所有写 Tool 都要有调用前条件检查、调用后状态确认、失败码、审计日志和补偿/撤销方案。

## 两周 MVP 路线

1. 第 1-2 天：锁定“新员工首周准备”一个 Case；定义任务、状态、SLA、依赖、验收物和高风险边界。
2. 第 3-5 天：实现协调 Agent + HR/IT/培训三个领域 Agent；使用结构化委托和回执，不允许自由文本串联。
3. 第 6-7 天：增加创建模拟工单、催办、人工审批三个写 Tool，并补充权限和幂等校验。
4. 第 8-10 天：做正常、IT 延迟、敏感信息拦截三条 Case；Trace 时间线、评测和回放同时完成。

## 验证指标

当前“20 名内部测试用户、90%满意度”只能证明小范围 MVP 可用，需准备其时间、口径和有效样本数。升级后优先测：Case 完成率、SLA 达成率、委托正确率、Tool 成功率、人工介入率、风险拦截准确率、异常恢复率，以及从首次请求到明确下一步的平均耗时。没有真实数据时，一律标注为待测。

# 企业入职助手：升级版面经

## 项目简介

这是一个面向新员工首周事务的 Agent 执行平台 MVP。当前系统已具备受约束的规划、RAG、确定性 Tool、风险审查、Trace 和评测；升级目标是以 Case、Task、领域 Agent 和审批策略，把“问答”变成可追溯的“入职交付协调”。

## 简历可用 bullet

- **受约束的任务编排：** 围绕入职咨询设计 Planner 与 Executor 分层链路，以能力白名单、依赖补全和必需节点校验约束模型选择，形成可回溯的 Agent 执行 MVP。
- **可信知识与事实锚定：** 区分 RAG 制度知识与员工、任务等确定性 Tool，要求回复生成基于查询证据，降低无依据生成风险。
- **风险分级与人工兜底：** 将敏感人事请求纳入确定性审核与转人工策略；后续扩展审批、幂等和写操作治理。
- **从问答到交付的状态建模：** 规划将设备、账号、材料和培训抽象为带负责人、依赖、SLA 和验收物的任务账本，为多 Agent 协作建立共同状态。
- **质量运营闭环：** 对能力配置、运行 Trace、用户反馈和 Bad Case 做可追溯管理；后续用任务级指标验证升级效果。

## 高频问题与口播

### 1. 为什么项目场景被认为简单，你如何优化？

我认同这个反馈。项目最初从高频入职咨询切入，所以展示重心在“识别问题、检索知识、生成回复”；如果用户只问一条制度，确实不能体现复杂协作。我的优化不是继续加页面，而是把目标改为“保障新员工首周关键事项按期完成”。用户提交一个入职目标后，系统创建 Case，拆出设备、账号、材料、培训等子任务，跟踪负责人、依赖、截止时间和验收物；发生阻塞时催办或转人工。这样回答只是过程中的一个界面，系统真正管理的是目标、任务、协作和异常。

追问：为什么不把所有模块都拆成 Agent？

我不会为了多 Agent 而拆分。只有任务确实存在专业边界、数据权限不同、交付物不同或可并行价值时，才应拆为领域 Agent。固定规则、字段校验和审批仍应由 Workflow 或程序负责。否则多个 Prompt 相互传话只会增加成本和排查难度，不能产生真实协作价值。

### 2. 当前项目是 Agent 还是 Workflow？

我会准确称它为单 Agent 执行平台 MVP。它已经有目标理解、规划、Tool 调用、RAG、风险审核和日志闭环；但其中很多模块是 Skill，不是各自具有独立身份、长期状态、权限和责任边界的 Agent。因此我不会把它包装成成熟多 Agent 系统。Skill 是可复用认知能力，Tool 是确定性接口，Workflow 是稳定受控步骤，Agent 才是对目标、能力选择和最终交付负责的主体。升级多 Agent 前，我会先把职责、权限、任务账本和验收补齐。

追问：如何证明多 Agent 不是 Prompt 串联？

我会让运行时对象证明它。每次委托都有 Case ID、Task ID、发送与接收 Agent 版本、授权范围、输入证据、截止时间和期望产物。领域 Agent 只能以结构化回执返回状态、结果、证据、风险和下一责任人；协调 Agent 只基于回执推进状态。这样可以回放谁委托谁、谁超时、谁验收和如何升级，才有可治理的协作。

### 3. 如何拆分多 Agent？

第一版我会设置一个入职协调 Agent 与三个领域 Agent。协调 Agent 负责接收目标、创建 Case、拆解任务、分派和汇总，不直接修改账号或解释全部制度；HR Agent 负责材料核验与制度依据；IT Agent 负责设备、账号、门禁工单和状态；培训 Agent 负责排期与报名。风险 Agent 横向校验权限与审批。拆分依据不是组织部门名称，而是不同的可交付物和权限域。所有 Agent 共享同一个 Case ID，但只有协调 Agent 对用户交付整体进展。

追问：A2A 消息需要什么字段？

最低包含 message_id、case_id、task_id、发送方/接收方身份和版本、目标、输入证据引用、可用权限、截止时间、期望交付物 Schema、优先级和幂等键。回执要补充状态、结构化结果、证据、置信度、风险等级、失败码、重试建议和下一责任人。对敏感数据只传最小必要字段，并在日志中脱敏。

### 4. 怎么做权限、安全和写操作？

我会明确读写分级。查询制度和待办可以自动执行；创建模拟 IT 工单、催办负责人属于中风险，需要用户身份、前置条件、频率限制和幂等键；账号权限、合同、薪资和个人敏感信息必须策略拦截并进入人工审批。权限不能只写在 Prompt 里，最终必须由 Tool 执行层基于 Agent 身份、用户授权、数据域和操作类型做确定性拒绝。每次写操作还要有前后状态校验、审计记录、失败码和必要的补偿/撤销机制。

追问：Tool 超时如何处理？

技术层只对明确可重试的错误有限重试，并用同一个幂等键先查结果，避免重复创建工单。业务层不能把超时说成已完成，而应把任务标记为待确认或 blocked，并创建人工确认或升级动作。用户看到的是事实状态和下一步，而不是系统掩盖失败后的自然语言。

### 5. 如何定义“任务完成”？

我不会以“生成了一段看起来合理的话”作为完成。比如“帮我准备下周一入职”，完成应能验证材料缺口和提交方式、设备和账号的状态与责任人、培训的排期或报名，以及阻塞时的升级记录。因此任务账本应有 not_started、in_progress、blocked、needs_approval、completed 和 cancelled 等状态，并定义每个状态转换条件和验收证据。满意度是体验指标；任务完成率、按时完成率、人工介入率和异常恢复率才更接近真实业务价值。

追问：如何防止 Agent 过度承诺？

在数据模型上严格区分“建议”“已发起”“已确认”“已完成”。没有 Tool 或领域 Agent 的证据，回复只能表达建议或待确认；不能自行提升任务状态。风险审核会检查敏感场景的确定性承诺，但根本约束来自任务状态机和执行层的证据校验。

### 6. RAG 的边界是什么？

RAG 负责回答制度、入职材料和流程说明“文档里怎么说”，并提供来源、版本和适用条件。它不应承担设备是否到位、账号是否开通、课程是否报名成功等实时事实，这些必须由业务 Tool 返回。RAG 也会有过期版本、切片遗漏、召回错误和文档冲突，所以要有导入复核、发布状态、版本管理、无答案兜底和回归用例。系统应把检索错误与生成错误分开记录，才能定位真实问题。

追问：如何评价 RAG？

我会建立覆盖高频制度、相似问法、冲突版本、无答案和敏感信息的标注问题集，先测正确文档是否进入候选集合，再测最终回答是否引用准确、是否遗漏适用条件、是否在证据不足时拒绝编造。线上结合低评分、追问率、人工纠错和知识缺口持续补充 Bad Case。

### 7. 如何做评测与运营？

我会按链路拆指标：意图路由是否正确、计划是否只调用授权能力、Tool 参数和结果是否正确、RAG 是否命中可信来源、回复是否完整合规、Case 是否在 SLA 内闭环。每个 Bad Case 都关联用户上下文、计划版本、Agent/Skill 版本、Tool 版本、输出和人工标注原因，修复后必须回归。多 Agent 还要增加委托正确率、消息 Schema 合规率、异常恢复率和人工接管率，不能只看最终文案的好坏。

追问：90% 满意度怎么解释？

我会如实说明，它来自 20 名内部测试用户的直接评价，证明小范围 MVP 的基础可用性，不能替代长期生产效果。面试前我会准备测试周期、有效评价数量、问题口径和低评分样本。下一阶段会将满意度作为体验层指标，同时引入任务完成、转人工、风险准确率和链路成功率等业务与系统指标。

### 8. 两周如何做升级 Demo？

我会压缩为“新员工首周准备”一个跨部门 Case。前两天定义子任务、状态、SLA、依赖、审批阈值和验收物；第三到第五天实现协调 Agent 与 HR/IT/培训 Agent 的结构化委托和回执；第六到第七天增加创建模拟工单、催办和人工审批的写 Tool；第二周用正常、IT 延迟、敏感信息拦截三类 Case 测试，并把 Trace 展示为时间线。即使接口先使用模拟数据，也必须让状态、权限、回执和异常处理真正运行，而不是预写展示文本。

追问：为什么不先做信用和结算？

信用、贡献和结算是值得探索的长期方向，但当前项目还没有足够的跨组织 Agent 协作和交易数据。第一阶段先做好身份、能力目录、权限、任务委托、验收、审计和可靠性画像更务实。等出现 Agent 被不同团队复用、成本需要分摊、服务质量要长期比较的需求，再验证积分、计费或结算机制。

### 9. 你个人的职责边界是什么？

我负责从业务问题到 MVP 验证的产品闭环：访谈 HR、行政和新员工，梳理高频咨询和人工边界；设计知识库清洗标准、业务 Tool、Agent 链路和风险策略；借助 Coze、Codex、Claude Code 快速完成原型与可运行系统，并做测试和演示。我能够阅读、调试代码和理解数据流，但不会把 AI 辅助开发的全部底层实现包装成完全手写。我的核心价值是将业务 SOP 拆解为可运行、可验证的 Agent 机制，并对场景、边界和验证结果负责。

追问：你最大的短板是什么？

我还没有长期运营大规模生产级多 Agent 网络的经历，这一点需要坦诚。但我不是停在概念层：已有真实业务调研、MVP、RAG、Tool、风险、Trace 和内部测试经验。下一步会用任务账本、权限审批和协作协议补齐机制。我的优势是流程拆解、0 到 1 验证和 AI 辅助工程实践；我希望在真实业务接口、可观测性和长期治理上继续快速积累。

### 10. AI+GIS 经历如何迁移到 Agent Manager？

AI+GIS 项目让我形成一个重要判断：不能因为有大模型就让模型包办全部流程。我将规划成果修改拆为面积统计、文本更新和图纸输出；确定性计算、字段映射和批处理由工具或程序完成，非结构化文本识别才由模型辅助，最终保留规则校验和人工复核。这和 Agent 产品完全一致：Agent 负责目标理解和能力编排，Tool 负责可靠执行，Workflow 负责稳定规则，人负责高风险和无法自动验收的判断。该经历证明我会按任务不确定性选择技术形态。

追问：如何解释“三天缩短到十分钟”？

我会清楚限定范围：这是在选定标准样例和既定输入条件下，方案调整后自动化输出相关成果的执行时长，不等同于包含沟通、数据修复与最终签审的完整项目周期。面试时应提供原步骤、自动化覆盖范围、输入输出和人工复核边界；如果没有长期多项目统计，应表述为业务验证结果，不能泛化承诺。

## 源码证据索引

| 主题 | 文件 | 对应口播 |
| --- | --- | --- |
| 规划与路由 | `server/planner.mjs` | 问题 2、7 |
| 逐步执行 | `server/executor.mjs` | 问题 2、3、7 |
| Tool Schema 与确定性查询 | `server/tool-registry.mjs` | 问题 4、6 |
| 风险审查 | `server/risk-review.mjs`、`server/privacy.mjs` | 问题 4、5 |
| 评测与测试 | `server/evaluation.mjs`、`tests/evaluation-v2.test.mjs` | 问题 7 |
| 任务和转人工数据 | `data/onboarding_tasks.json`、`data/handoffs.json` | 问题 1、5、8 |
